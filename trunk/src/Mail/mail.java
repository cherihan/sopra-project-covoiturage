/*

package Mail;

import javax.websocket.Session;
import java.util.Properties;
import com.sun.corba.se.impl.protocol.giopmsgheaders.Message;

public class mail extends Thread {

	
	public mail() {
		
		 L'adresse IP de votre serveur SMTP 
		String smtpServer = "";

		/* L'adresse de l'expéditeur 
		String from = "manolias@etud.insa-toulouse.fr";

		/* L'adresse du destinataire PARAM
		String to = "";

		/* L'objet du message PARAM 
		String objet = "";

		/* Le corps du mail 
		String texte = "Texte du mail";

		Properties props = System.getProperties();
		props.put("mail.smtp.host", smtpServer);
		// Session encapsule pour un client donné sa connexion avec le serveur de mails
		Session session = Session.getDefaultInstance(props, null);

		Création du message
		Message msg = new MimeMessage(session);

		try {
		      msg.setFrom(new InternetAddress(from));
		      msg.setRecipients(Message.RecipientType.TO,InternetAddress.parse(to, false));
		      msg.setSubject(objet);
		      msg.setText(texte);
		      msg.setHeader("X-Mailer", "LOTONtechEmail");
		      Transport.send(msg);
		}
		catch (AddressException e) {
		      e.printStackTrace();
		} 
		catch (MessagingException e) {
		      e.printStackTrace();
		} 
	} 
}
*/


		
